<Window x:Class="WpfSynthPiano.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WPF Piano" Height="650" Width="1300"
        Background="#18181A"
        Icon="/icon.png"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="{x:Type Control}">
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <SolidColorBrush x:Key="WindowBackground" Color="#18181A" />
        <SolidColorBrush x:Key="PanelBackground" Color="#252526" />
        <SolidColorBrush x:Key="BorderColor" Color="#3E3E3E" />
        <SolidColorBrush x:Key="PrimaryTextColor" Color="#E0E0E0" />
        <SolidColorBrush x:Key="AccentColor" Color="#0096C7" />
        <SolidColorBrush x:Key="AccentHoverColor" Color="#00B4D8" />
        <SolidColorBrush x:Key="ButtonBackground" Color="#3A3A3C" />
        <SolidColorBrush x:Key="ButtonHoverBackground" Color="#4A4A4C" />
        <SolidColorBrush x:Key="ButtonForeground" Color="#E0E0E0" />
        <SolidColorBrush x:Key="ButtonDisabledBackground" Color="#2A2A2C" />
        <SolidColorBrush x:Key="ButtonDisabledForeground" Color="#7A7A7C" />

        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="{StaticResource ButtonBackground}" />
            <Setter Property="Foreground" Value="{StaticResource ButtonForeground}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentColor}" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource ButtonDisabledBackground}" />
                    <Setter Property="Foreground" Value="{StaticResource ButtonDisabledForeground}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PianoKeyStyle" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E0E0E0" />
            <Setter Property="Foreground" Value="#000" />
            <Setter Property="BorderBrush" Value="#6A6A6A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,0,5,5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Bottom"
                                              Margin="0,0,0,10"
                                              TextElement.FontSize="10" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A0A0A0" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="BlackPianoKeyStyle" BasedOn="{StaticResource PianoKeyStyle}">
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderBrush" Value="#1A1A1A" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,0,5,5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Bottom"
                                              Margin="0,0,0,5"
                                              TextElement.FontSize="8" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#505050" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelTextStyle">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,5,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <!-- Dark Theme ComboBox Brushes -->
        <SolidColorBrush x:Key="ComboBoxNormalBorderBrush" Color="#555555" />
        <SolidColorBrush x:Key="ComboBoxNormalBackgroundBrush" Color="#2D2D2D" />
        <SolidColorBrush x:Key="ComboBoxDisabledForegroundBrush" Color="#6D6D6D" />
        <SolidColorBrush x:Key="ComboBoxDisabledBackgroundBrush" Color="#252525" />
        <SolidColorBrush x:Key="ComboBoxDisabledBorderBrush" Color="#444444" />

        <!-- ComboBoxItem Style for Dark Theme -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        SnapsToDevicePixels="True">
                            <ContentPresenter
                        Margin="4"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#444444"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#555555"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource ComboBoxDisabledForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate TargetType="ToggleButton" x:Key="ComboBoxToggleButtonTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border Grid.ColumnSpan="2" Name="Border"
              BorderBrush="{StaticResource ComboBoxNormalBorderBrush}" 
              CornerRadius="0" BorderThickness="1, 1, 1, 1" 
              Background="{StaticResource ComboBoxNormalBackgroundBrush}" />
                <Border Grid.Column="1" Margin="1, 1, 1, 1" BorderBrush="#444" Name="ButtonBorder"
              CornerRadius="0, 0, 0, 0" BorderThickness="0, 0, 0, 0" 
              Background="{StaticResource ComboBoxNormalBackgroundBrush}" />

                <Path Name="Arrow" Grid.Column="1" 
            Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
            HorizontalAlignment="Center" Fill="#444"
            VerticalAlignment="Center" />
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                    <Setter Property="Panel.Background" TargetName="ButtonBorder" Value="WhiteSmoke"/>
                </Trigger>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                    <Setter Property="Panel.Background" TargetName="ButtonBorder" Value="WhiteSmoke"/>
                    <Setter Property="Shape.Fill" TargetName="Arrow" Value="#FF8D979E"/>
                </Trigger>
                <Trigger Property="UIElement.IsEnabled" Value="False">
                    <Setter Property="Panel.Background" TargetName="Border" Value="{StaticResource ComboBoxDisabledBackgroundBrush}"/>
                    <Setter Property="Panel.Background" TargetName="ButtonBorder" Value="{StaticResource ComboBoxDisabledBackgroundBrush}"/>
                    <Setter Property="Border.BorderBrush" TargetName="ButtonBorder" Value="{StaticResource ComboBoxDisabledBorderBrush}"/>
                    <Setter Property="TextElement.Foreground" Value="{StaticResource ComboBoxDisabledForegroundBrush}"/>
                    <Setter Property="Shape.Fill" TargetName="Arrow" Value="#999"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="StyledComboBox"  TargetType="{x:Type ComboBox}">
            <Setter Property="UIElement.SnapsToDevicePixels" Value="True"/>
            <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="TextElement.Foreground" Value="White"/>
            <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Grid.Column="2"
                                ClickMode="Press" Focusable="False"
                                IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                Template="{StaticResource ComboBoxToggleButtonTemplate}"/>

                            <ContentPresenter Name="ContentSite" Margin="5, 3, 23, 3" IsHitTestVisible="False"
                              HorizontalAlignment="Left" VerticalAlignment="Center"                              
                              Content="{TemplateBinding ComboBox.SelectionBoxItem}" 
                              ContentTemplate="{TemplateBinding ComboBox.SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <TextBox Name="PART_EditableTextBox" Margin="3, 3, 23, 3"                     
                                 IsReadOnly="{TemplateBinding IsReadOnly}"
                                 Visibility="Hidden" Background="Transparent"
                                 HorizontalAlignment="Left" VerticalAlignment="Center"
                                 Focusable="True" >
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox" >
                                        <Border Name="PART_ContentHost" Focusable="False" />
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <!-- Popup showing items -->
                            <Popup Name="Popup" Placement="Bottom"
                               Focusable="False" AllowsTransparency="True"
                               IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}"
                               PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding FrameworkElement.ActualWidth}"
                                    MaxHeight="{TemplateBinding ComboBox.MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="{StaticResource ComboBoxNormalBackgroundBrush}" Margin="0, 1, 0, 0"
                                        CornerRadius="0" BorderThickness="1,1,1,1" 
                                        BorderBrush="{StaticResource ComboBoxNormalBorderBrush}"/>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ItemsControl.HasItems" Value="False">
                                <Setter Property="FrameworkElement.MinHeight" TargetName="DropDownBorder" Value="95"/>
                            </Trigger>
                            <Trigger Property="UIElement.IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" Value="{StaticResource ComboBoxDisabledForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="ItemsControl.IsGrouping" Value="True">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            </Trigger>
                            <Trigger Property="ComboBox.IsEditable" Value="True">
                                <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
                                <Setter Property="UIElement.Visibility" TargetName="PART_EditableTextBox" Value="Visible"/>
                                <Setter Property="UIElement.Visibility" TargetName="ContentSite" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox" x:Key="StyledCheckBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <Style TargetType="Slider" x:Key="StyledSlider">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource AccentColor}" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="150" />
            <Setter Property="Margin" Value="10,0,0,0" />
        </Style>

        <Style TargetType="Button" x:Key="IconButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style TargetType="ListView" x:Key="StyledListView">
            <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Width" Value="500"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <Style TargetType="ProgressBar" x:Key="StyledProgressBar">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Background="{TemplateBinding Background}">
                            <Grid x:Name="PART_Track" ClipToBounds="true">
                                <Rectangle x:Name="PART_Indicator"
                                   Fill="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Left"
                                   RadiusX="4"
                                   RadiusY="4" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Dark Theme Brushes for ListView -->
        <SolidColorBrush x:Key="ListViewBackgroundBrush" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="ListViewBorderBrush" Color="#555555"/>
        <SolidColorBrush x:Key="ListViewItemHoverBrush" Color="#444444"/>
        <SolidColorBrush x:Key="ListViewItemSelectedBrush" Color="#555555"/>
        <SolidColorBrush x:Key="ListViewItemDisabledBrush" Color="#6D6D6D"/>

        <!-- ListViewItem Style -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" 
                        BorderThickness="0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                      Margin="4" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ListViewItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ListViewItemSelectedBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource ListViewItemDisabledBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView Style -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="{StaticResource ListViewBackgroundBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource ListViewBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <!-- Dark Theme ScrollBar Brushes -->
        <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#555555"/>
        <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#777777"/>
        <SolidColorBrush x:Key="ScrollBarThumbPressedBrush" Color="#999999"/>

        <!-- ScrollBar Style -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{StaticResource ScrollBarTrackBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" Background="{TemplateBinding Background}">
                            <Track Name="PART_Track" IsDirectionReversed="true"
                           Focusable="false">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand"
                                          Style="{x:Null}" 
                                          IsTabStop="False"
                                          Background="{StaticResource ScrollBarTrackBrush}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Background="{StaticResource ScrollBarThumbBrush}" 
                                   BorderThickness="0">
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Background" Value="{StaticResource ScrollBarThumbBrush}"/>
                                                <Setter Property="Height" Value="12"/>
                                                <Setter Property="Width" Value="12"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="{TemplateBinding Background}" 
                                                            CornerRadius="2"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource ScrollBarThumbHoverBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource ScrollBarThumbPressedBrush}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand"
                                          Style="{x:Null}"
                                          IsTabStop="False"
                                          Background="{StaticResource ScrollBarTrackBrush}"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <DockPanel LastChildFill="True">

        <Border DockPanel.Dock="Top" Background="{StaticResource PanelBackground}" Padding="15" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,15">
                    <TextBlock Text="Instrument:" Style="{StaticResource LabelTextStyle}" />
                    <ComboBox Name="InstrumentComboBox"
                              Style="{StaticResource StyledComboBox}"
                              Width="220"
                              SelectionChanged="InstrumentComboBox_SelectionChanged" />

                    <TextBlock Text="Wave Type:" Style="{StaticResource LabelTextStyle}" Margin="20,0,0,0" />
                    <ComboBox Name="WaveTypeComboBox"
                              Style="{StaticResource StyledComboBox}"
                              Width="140"
                              SelectionChanged="WaveTypeComboBox_SelectionChanged" />

                    <TextBlock Text="Volume:" Style="{StaticResource LabelTextStyle}" Margin="20,0,0,0" />
                    <Slider Name="VolumeSlider"
                            Style="{StaticResource StyledSlider}"
                            Minimum="0" Maximum="1" Value="0.05"
                            SmallChange="0.05" LargeChange="0.1"
                            ValueChanged="VolumeSlider_ValueChanged" />
                    <Label Name="VolumeValueLabel"
                           Content="5%"
                           Foreground="{StaticResource PrimaryTextColor}"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,15">
                    <CheckBox Name="ShowLabelsCheckbox"
                              Content="Show Note Labels"
                              IsChecked="True"
                              Style="{StaticResource StyledCheckBox}"
                              Checked="ToggleLabels"
                              Unchecked="ToggleLabels" />

                    <TextBlock Text="Octave Range:" Style="{StaticResource LabelTextStyle}" Margin="20,0,0,0" />
                    <ComboBox Name="FromOctaveComboBox"
                              Style="{StaticResource StyledComboBox}"
                              Width="70"
                              SelectionChanged="OctaveRangeChanged" />
                    <TextBlock Text="to" Style="{StaticResource LabelTextStyle}" Margin="5,0" />
                    <ComboBox Name="TillOctaveComboBox"
                              Style="{StaticResource StyledComboBox}"
                              Width="70"
                              SelectionChanged="OctaveRangeChanged" />

                    <TextBlock Text="MIDI Out:" Style="{StaticResource LabelTextStyle}" Margin="20,0,0,0" />
                    <ComboBox Name="MidiOutComboBox"
                              Style="{StaticResource StyledComboBox}"
                              Width="220"
                              SelectionChanged="MidiOutComboBox_SelectionChanged" />

                    <TextBlock Text="MIDI In:" Style="{StaticResource LabelTextStyle}" Margin="10,0,0,0" />
                    <ComboBox Name="MidiInComboBox"
                              Style="{StaticResource StyledComboBox}"
                              Width="220"
                              SelectionChanged="MidiInComboBox_SelectionChanged" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Name="RecordButton" Style="{StaticResource ModernButton}"
                            Content="Record"
                            Width="120" Height="30" Margin="0,0,10,0"
                            Click="RecordButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Image Source="/Assets/record.png" Width="16" Height="16" Margin="0,0,5,0" />
                                        <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Name="PlayButton" Style="{StaticResource ModernButton}"
                            Content="Play"
                            Width="120" Height="30" Margin="0,0,20,0"
                            Click="PlayButton_Click" IsEnabled="False">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Image Source="/Assets/play.png" Width="16" Height="16" Margin="0,0,5,0" />
                                        <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- Songs List and Playback -->
                    <TextBlock Text="Songs:" Style="{StaticResource LabelTextStyle}" Margin="30,0,0,0" VerticalAlignment="Center"/>
                    <ListView Name="SongsListView"
                        Width="500"
                        Height="80"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        SelectionMode="Single"/>

                    <Button Name="PlaySongButton"
                            Content="Play Selected"
                            Style="{StaticResource ModernButton}"
                            Width="120" Height="30"
                            Margin="10,0,10,0"
                            Click="PlaySongButton_Click" />

                    <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Name="SongTimeText" Text="00:00 / 00:00"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,5"
                                   Foreground="{StaticResource PrimaryTextColor}" />
                        <ProgressBar Name="SongProgressBar"
                                     Style="{StaticResource StyledProgressBar}"
                                     Width="300"
                                     Minimum="0" Maximum="1" Value="0" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="3*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#1E1E1E"
                    Margin="0,0,0,5"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="4">
                <Canvas Name="OscilloscopeCanvas" />
            </Border>

            <Border Grid.Row="1"
                    Background="{StaticResource PanelBackground}"
                    Margin="0,5,0,0"
                    Padding="10"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="4">
                <Canvas Name="PianoCanvas" />
            </Border>
        </Grid>
    </DockPanel>
</Window>